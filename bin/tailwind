#!/usr/bin/env bash
set -e

# Compile the DataDrip engine's Tailwind CSS in watch mode (development).
# For a one-shot build use `bundle exec rake data_drip:css`.
cd "$(dirname "$0")/.."

[ -d node_modules ] || npm install --no-audit --no-fund

npm run watch:css
