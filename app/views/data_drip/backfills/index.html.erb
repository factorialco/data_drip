<ul>
    <% @backfills.map(&:name).uniq.each do |backfill| %>
    <li>
        <%= backfill %> 
        <%= button_to 'Run', run_backfill_path(backfill: backfill), method: :post, class: 'btn btn-primary' %>
    </li>
    <% end %>
</ul>
